let o1 = { foo: 'bar' };
let o2 = { foo: 'bar' };
let o3 = o2;

console.log(o3);

// o3 has now the value of the o2 
// we can't write o2 = o3 because o3 doesn't have any value and we already assigned a value to o2

